#include <stdio.h>
#include <stdlib.h>
#include <string.h>

//static 关键字的作用
//1. 在函数内部声明的静态变量在函数调用结束后不会被销
//毁，而是保留其值，直到下一次函数调用。
//2. 在文件作用域中声明的静态变量和函数只能在当前文件
//中访问，不能被其他文件访问，这有助于避免命名冲突。


int main() {

    return 0;
}
